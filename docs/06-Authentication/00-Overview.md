# Authentication Overview

FlowPilot uses Meta OAuth as the only login mechanism.

Goals:
- No passwords
- Session-based JWT authentication
- Rotating refresh tokens
- Multi-device support
- Secure logout and token revocation
